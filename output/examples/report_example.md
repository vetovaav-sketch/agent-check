# Отчет по соответствию требований

## Реализовано неправильно

### R2
**Требование:** Система должна иметь функцию вычитания двух чисел.

**Проблема:** Реализация вычитания найдена, но логика операции противоречит требованию.

**Почему это нарушение:** Функция `subtract` возвращает сумму (`a + b`), а не разность (`a - b`).

**Confidence:** 0.92

**Доказательства:**
- `calculator.py:5-7`
```
def subtract(a: int, b: int) -> int:
    # BUG: специально неверная реализация для демонстрации отчёта
    return a + b
```

## Отсутствует или не найдено в коде

### R3
**Требование:** Система должна сохранять историю операций в файл history.log.

**Проблема:** Подтверждение реализации не найдено в проверенном коде.

**Почему это нарушение:** В репозитории нет записи в файл `history.log` и нет функций логирования операций.

**Confidence:** 0.86

**Доказательства:**
- Нет явных доказательств в доступных файлах.
